# Кириллические имена тестов порождают class-файлы с кириллицей в имени —
# без UTF-8 в jnu.encoding JVM не может создать такой путь на диске.
org.gradle.jvmargs=-Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8
kotlin.daemon.jvmargs=-Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8
