# Состояние работы над разделом `/suppliers`

Обновляется на каждом существенном этапе, чтобы работа не терялась при сжатии
контекста сессии.

## Где мы

**Этап 0 из 7**, аудит закрыт, первая поломка исправлена.

| этап | состояние |
|---|---|
| Аудит и проектные документы | **сделано** 20.09.2026 |
| 0. Починка обесценивающих дефектов | 1 из 7 пунктов |
| 1. Канонический Supplier Master | не начато |
| 2. Раздел только на чтение | не начато |
| 3. Котировка как документ, артикулы | не начато |
| 4. Рабочая таблица спецификации | не начато |
| 5. Подбор адресатов с объяснением | не начато |
| 6. Условия, юрлица, очередь решений | не начато |
| 7. Инкрементальное обновление | не начато |

## Что сделано 20.09.2026

**Аудит.** 14 направлений + 6 подпроектов вне манифеста, 84 агента, каждый
критичный риск проверен отдельным скептиком на опровержение. Из 64 заявленных
рисков 13 опровергнуто, 51 подтверждён (3 critical, 18 high, 27 medium, 3 low).

**Документы** (`docs/suppliers/`): `CURRENT_STATE.md`, `DATA_SOURCE_MAP.md`,
`GAP_ANALYSIS.md`, `TARGET_ARCHITECTURE.md`, `DATA_MODEL.md`,
`METRIC_DICTIONARY.md`, `PERMISSION_MATRIX.md`, `IMPLEMENTATION_PLAN.md`,
`DECISIONS.md`, `STATE.md`.

**Исправление.** `base/fetch_rfq.py` — выгрузка СП-166 забирала одну страницу
вместо всех (`while not items:`). Измерено: 50 карточек из 127 на придуманном
корпусе. Закрыто тремя тестами `tests/test_rfq_pagination.py`, проверено
падением на откаченном коде.

**Решения владельца:** Р-1 данные только за входом, Р-2 платежей нет,
Р-3 юрлиц несколько, Р-4 права постепенно. Подробно — `DECISIONS.md`.

## Следующий шаг

Пункт 0.2: ключ поставщика — `rfq.supplier_id` вместо текста названия.
`base/suppliers.py:47` использует `supplier TEXT PRIMARY KEY`, хотя Bitrix
`companyId` уже лежит в `rfq.supplier_id` (`base/fetch_rfq.py:52`) и просто не
попал в SELECT модуля. Цена — 4 часа, разблокирует четыре вопроса ТЗ из двадцати.

## Базовая линия

Замер 20.09.2026, до изменений:

```
pytest       зелёный, ~1090 тестов, 11 пропущено
ruff         чисто
каталог      актуален, 256 наборов
указатель    актуален, 41 592 значения
```

После изменений: те же плюс 3 теста `tests/test_rfq_pagination.py`.

В окружении сессии не было `pytest` и `psycopg2` — поставлены; без `psycopg2`
два теста `test_library_ocr.py` падают импортом, это свойство окружения, а не
кода.

## Известные блокеры

| блокер | кто снимает |
|---|---|
| список наших юрлиц | владелец |
| назначение полей «Оплачено» / «Остаток к оплате» | владелец или зонд портала |
| применена ли `migrations_rls.sql` к живой базе ЗИП | проверка в Supabase |
| живое состояние прав в KV `acl:v1` | панель `/admin` |
| нет staging: каждый мерж — деплой на живой сайт | владелец, если нужен preview-проект |

## Хвосты репозитория

15 открытых PR, из них 8 старше двух недель. Дата самого старого — 28.07.2026
(#67 «КВАНТ Скан»). Шесть — обновления зависимостей от dependabot (#183–#188).
Невлитые ветки `claude/*` — более сорока. Разбор хвостов в этот аудит не входил.
