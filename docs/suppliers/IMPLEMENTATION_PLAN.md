# План реализации

Оценка ТЗ — 300–500 часов. Разбито так, чтобы каждый этап давал работающий
вертикальный срез, а не слой, который нельзя проверить.

## Этап 0. Починка того, что обесценивает данные (сделано частично)

Дёшево, обратимо, разблокирует всё остальное.

| # | что | статус | цена |
|---|---|---|---|
| 0.1 | пагинация СП-166: `while not items` → полный проход | **сделано**, 3 теста | 1 ч |
| 0.2 | ключ поставщика — `rfq.supplier_id` вместо текста названия | **заблокировано**, см. ниже | 4 ч |
| 0.3 | сторона документа в джойне `brand_suppliers` — не зачитывать наш «Request file» | **сделано**, 2 теста | 2 ч |
| 0.4 | стадии: переименование больше не пропадает молча, разряд «не выбрали» заработал | **сделано частично**, 3 теста | 3 ч |
| 0.5 | `days_med` переименовать в `days_to_stage_move` и убрать из вида «срок ответа» | **сделано**, тест на имя колонки | 1 ч |
| 0.6 | гейт учит шаблоны ключей Supabase (`sb_publishable_`, `sb_secret_`) | **заблокировано**, см. ниже | 1 ч |
| 0.7 | кириллические двойники в `base/` — довести исправление от 18.09 | **сделано**, 8 тестов | 2 ч |

**0.5 важнее, чем кажется.** Пока метрика называется «срок ответа поставщика»,
ею пользуются как сроком ответа поставщика. Переименование честнее, чем сноска.

## Этап 1. Канонический Supplier Master (25–40 ч)

1. **Сделано.** Таблицы `sup_entity`, `sup_identifier`, `sup_fact`, `sup_override`,
   `sup_review`, `sup_number_registry`, `our_entity` и представление
   `sup_effective` — `library/supabase/suppliers_schema.sql`. Защита по образцу
   `archive_*`: `FORCE RLS`, полный `REVOKE`, гранты только `service_role`.
   Применена на чистой базе, идемпотентна, закрыта восемью тестами
   (`tests/test_suppliers_schema_sql.py`). Автоматически ни одним workflow не
   применяется: сведение делается только после замера.
2. Выдача вечных номеров `KV-S-…` по алгоритму `pnw/tools/kv_number.py`, реестр
   по устойчивому ключу — номера не переиздаются.
3. Сведение шести реестров: Bitrix `companyId` → `lib_suppliers` → `pnw` →
   `dossiers` → `ship_sellers` → `supplier_crm`. Все совпадения — строками
   `sup_identifier`, ни одно имя не перезаписывается.
4. **Измерено 20.09.2026** — `scripts/supplier_registry_overlap.py`, пять файловых
   реестров (шестой, Bitrix, ждёт прогона владельца):

   | что | число |
   |---|---:|
   | строк во всех файловых реестрах | 3 874 |
   | разных нормализованных имён | 2 890 |
   | схлопнулось бы по имени | 984 |
   | досье ГТУ ∩ pnw по имени | 878 |
   | ship_sellers ∩ pnw по имени | 17 |
   | ship_sellers ∩ pnw **по домену** | **41** |
   | доменов с несколькими написаниями | 61 (144 написания) |
   | имён с разными доменами | 4 из 2 394 — 0,17 % |

   **Правило сведения выведено из этих чисел, а не придумано:**

   1. совпал домен → слияние, `evidence` = домен, `status` = `verified`;
   2. совпало имя и домены не противоречат → слияние, `status` = `inferred`;
   3. совпало имя, а домены разные → **не слияние**: строка в `sup_review`,
      `kind` = `ambiguous_match`. Таких 4;
   4. совпало только имя, домена нет ни у одной стороны → `status` = `candidate`,
      ждёт подтверждения человеком.

   Домен обязателен как основной признак: он находит 144 написания, которых
   сведение по имени не видит (`parkerhannifincorporation` и
   `parkerhannifingasturbine` — одна компания, один сайт). Обратная опасность
   мала, 0,17 %, но не нулевая, поэтому пункт 3 существует.

   **Самое слабое место — досье ГТУ:** 878 совпадений по имени и ни одного сайта
   во всём наборе, то есть проверить их доменом нечем. Это разряд `candidate`
   целиком, и он же самый крупный.
5. Холостой прогон обязателен; запись отменяется сама при непройденных гейтах
   (`CLAUDE.md`, правило 3).

**Готово, когда:** у каждого поставщика из шести реестров есть один `sup_id`,
пересечение измерено числом, а не на глаз, и повторный прогон не создаёт дублей.

## Этап 2. Раздел только на чтение (20–30 ч)

Маршрут `/suppliers` в `public/_worker.js` по образцу `/library`, страница без
данных, `/api/suppliers/*` с серверной фильтрацией по правам. Список, карточка,
хронология из `rfq` и `activities`.

Права `suppliers`, `suppliers_pii`, `suppliers_fin`, `suppliers_edit` в
`access/acl.js` — с тестами в `access/test/`.

**Готово, когда:** сотрудник открывает поставщика, видит историю запросов и
ответов, а человек без `suppliers_pii` не получает контакты ни через экран, ни
прямым обращением к API.

## Этап 3. Котировка как документ и артикулы (30–45 ч)

`sup_quote`, `sup_quote_line` с `doc_side`, оригинальным артикулом, валютой,
единицей и `price_basis`. Одно правило нормализации артикула **с версией**, вместо
шести нынешних; обратная совместимость проверяется на существующем корпусе.

Перенос трёх осей наличия целиком — `verdict`, `stock_grade`, `covers_qty` — с
датой проверки. Обе недостающие колонки заводятся сразу.

**Готово, когда:** строка с неизвестной валютой или базисом цены видна, но
исключена из сравнения с текстовой причиной; сумма по `covers_qty = full`
воспроизводит замер 144 445 USD.

## Этап 4. Рабочая таблица спецификации (25–40 ч)

Главный рабочий экран: вставка сотен артикулов из Excel, поиск по многим номерам
сразу, закреплённые заголовки и колонки, клавиатура, устойчивое выделение при
виртуализации, сортировка по всей выборке, возврат из карточки с сохранением
позиции, боковая панель с источником.

Образца в репозитории нет: `library.html` — это карточки, не таблица на сотни
строк. Делается заново.

**Готово, когда:** сотрудник вставляет 100+ номеров, работает клавиатурой,
уходит в карточку и возвращается без потери фильтров, прокрутки и выделения.

## Этап 5. Подбор адресатов с объяснением (20–30 ч)

Иерархия доказательств из ТЗ §8.2, наполненная нашим методом: exact PN →
verified manufacturer → машина/семейство → изготовитель узла → внутренние
исследования → внешний поиск. Уровни не смешиваются.

У каждого кандидата: почему попал, уровень доказательства, последнее событие,
числитель и знаменатель отзывчивости, что известно и чего не хватает.

**Готово, когда:** по строке заявки система называет адресатов, и по каждому
числу можно дойти до породившей записи.

## Этап 6. Условия, юрлица, очередь решений (30–50 ч)

`our_entity`, `sup_terms` с областью действия и сроком, очередь решений
руководителя. Заполняется, когда владелец назовёт список юрлиц; колонки
заложены раньше.

## Этап 7. Инкрементальное обновление (25–40 ч)

Сегодня инкрементальности нет нигде: фильтр только по дате создания,
`DATE_MODIFY` сохраняется, но не используется. Вводится фильтр по изменению, кеш
по хешу содержимого и версии правила, `run_id` в каждой записи, отчёт прогона:
получено / создано / обновлено / пропущено / ошибок / в проверку.

**Готово, когда:** повторный прогон даёт тот же результат, один битый документ
не роняет пакет, корректировка человека переживает обновление, а противоречие
попадает в очередь, а не выбирается молча.

## Этап 8. Карточки бренда, ODM-поставщика и трейдера (80–130 ч)

Поставлен распоряжением владельца 23.09.2026, сверх оценки ТЗ: «Мне будут нужны
карточки, аналогичные карточке товара, но по брендам (какие агрегаты входят,
какие машины, применение), по ODM-поставщикам, предлагающим аналоги, и по
поставщикам-трейдерам, чтобы не терять всю информацию».

Образец — карточка товара `/nomenclature` (22.09.2026, `library/crossref.py`,
`public/nomenclature.html:415-520`). От неё берутся три правила: у каждого
утверждения подписан источник; четыре утверждения об изготовителе (каталог,
слово поставщика в файле, наша карточка запроса, «чей это номер на самом деле»)
не сводятся в одно поле; применимость по машине показывается с оговоркой, что
она не подтверждена.

**Место в цепочке.** Бренд стоит над звеньями «машина» и «узел»: по шильдику
сначала опознаётся бренд, от него — машины (`lib_models`, 132), узлы
(`lib_units`, 126), детали (`lib_parts`) и исполнители. ODM-поставщик и трейдер
относятся к звену «исполнитель» (`lib_suppliers` — 4 535, `lib_part_suppliers` —
4 726, из них 433 с проверкой наличия) и ссылаются назад: на «запчасть» — какие
коды закрывают — и через неё на «машину». В потоке сделки карточки работают на
подбор адресатов (этап 5) и на котировку: ни один факт из КП не остаётся без
адресата. Из вопросов ТЗ закрывают часть вопросов 7 («кто OEM и реальный
изготовитель»), 17 («кому отправить RFQ и почему») и 18 («где sourcing
intelligence отсутствует»), `GAP_ANALYSIS.md:22,41,42`.

**Что мешает сегодня** (разведка 23.09.2026, только чтение):

1. Сущности «бренд» в рабочем контуре нет. Бренд записан свободным текстом в
   восьми колонках: `lib_models.oem`, `lib_parts.oem`, `lib_demand.oem`,
   `lib_prices.oem`, `lib_customs.brand`, `lib_part_alt.alt_maker`,
   `lib_pn_patterns.oem`, `lib_suppliers.oem_brands`. Нормализуют его пять
   несогласованных правил: `library/equipment.py` (`OEM_JUNK`, `OEM_ALIAS`),
   `scripts/build_dict.py` (`nkey`), `library/load_suppliers.py` (`norm`),
   `scripts/supplier_registry_overlap.py` (`norm_name`),
   `base/extract_positions.py` (`load_brands`). `lib_prices.rfq_brands` хранит
   ключи портала без имён, а справочник марок СП-176 (2 449) читает только
   офлайн-корпус `base/`.
2. Роль компании не хранится нигде как проверенный признак. `lib_suppliers.kind` —
   свободный текст: холостой прогон `library/load_suppliers.py` 23.09.2026 дал
   241 разное значение, и лишь одно из них содержит слово ODM.
   `sup_entity.roles` не заполняет никто, у ребра `lib_part_suppliers` роли нет.
   Словарей ролей два, и они несовместимы: `OEM | ODM | дистрибьютор | сервис | трейдер`
   (`library/supabase/schema.sql:375`) и
   `manufacturer | distributor | trader | service | logistics`
   (`library/supabase/suppliers_schema.sql:42`).
3. Реестр исполнителей (`lib_suppliers`, разведка) и реестр компаний портала
   (`sup_entity`, Bitrix) не сведены (`DATA_SOURCE_MAP.md:30`,
   `library/crossref.py:31-36`). В плане этапа 1 (п. 3) `lib_suppliers` стоит
   вторым, но сведение его не читает: `library/load_supplier_master.py` берёт
   портал и четыре файловых реестра из `scripts/supplier_registry_overlap.py:54`.
   Поэтому роль компании, приславшей КП, неизвестна, а у ODM из разведки нет
   истории наших запросов.
4. Снимок `crossref:v1` занимает 78,9 % предела 8 МиБ (`library/crossref.py:92`).
   Новые карточки строятся отдельными ключами, а не новыми полями в нём.

### Одна карточка компании с разделами по роли

ODM-поставщик и трейдер — не две сущности, а роли одной компании, и роль берётся
относительно бренда: одна компания бывает дилером по одной марке и трейдером по
другой. Поэтому «карточка ODM» и «карточка трейдера» — разделы карточки компании
на `/suppliers`, которые включаются фактом роли. К ним — два списка: «ODM-поставщики
аналогов» и «трейдеры», с фильтром по бренду и по статусу роли.

Отдельные страницы отвергнуты по той же причине, по которой у номенклатуры один
снимок на две карточки: две сборки расходятся, и карточка ODM покажет котировку,
которой нет в карточке компании. У бренда свой объект, поэтому карточка бренда —
отдельная страница `/brands`.

### Карточка бренда: поля и источники

**Есть** — данные в рабочей базе и читаются. **Частично** — есть не у всех, лежат
только файлом или без сведения написаний. **Нет** — источника нет или его не
читают.

| поле | источник | состояние |
|---|---|---|
| имя и все написания | `dict/oem.json` (304 ключа, у 239 больше одного написания) | частично: файл, в базе нет |
| владелец, прежние имена, действующие и снятые линейки, правило номера | `zip/data/oem_atlas.json` (101 изготовитель, правило номера у 93) | частично: файл; разведано 7 пар «направление × регион» из 18 |
| элемент справочника марок портала | СП-176 «Brands», 2 449 (`base/fetch_brands.py`) | нет: только офлайн-корпус |
| машины | `lib_models.oem` (132); ГШО — `zip/data/machines.json` (6 брендов, 76 машин); ГПУ — `gpu/data/machines.json` | есть для 132; ГШО и ГПУ — файлами |
| агрегаты и узлы | `lib_models` → `lib_part_models` (9 869) → `lib_parts.unit_id` → `lib_units`; узлы ГШО — `zip/data/machines.json`, поле `nodes` | частично: у 21 % позиций узел не определён; дерево узлов общее на ГТУ и на ГПУ, а не своё у бренда |
| состав машины | `lib_bom` (176 строк) | частично: одна машина из 77 |
| кто делает узлы под брендом | `dict/chain.json` (311: 274 изготовителя и 37 указаний к закупке), `zip/data/subsupplier_recon.json` (150 цепочек), `gpu/data/subsuppliers.json` (63) | частично: файлы, в базе нет |
| применение | `lib_models.use_case` (текст), `lib_fleet` (66 площадок с владельцем) | частично: у площадок ГПУ машина не названа |
| отрасль заказчика | `INDUSTRY` компании Bitrix | нет: выгружается только в офлайн-корпус (`base/export_bitrix.py:112`); у `lib_demand` заказчика нет |
| детали и коды | `lib_parts` по `oem`, `lib_parts.aliases`, `lib_pn_patterns`, `pnw/data/numbers.json` (номеров бренда 13 309) | частично: номера — файлом; `lib_pn_patterns.oem` — заголовок раздела текстом |
| перешильдовка: чей номер на самом деле | `lib_part_alt` при `kind='номер изготовителя'`; `pnw/data/item_master.json` (brand ≠ maker у 2 334 деталей) | частично |
| спрос | вид `lib_demand_catalog` (спрос × каталог по `lib_pn_key`); `lib_demand.oem` | частично: через каталог — только позиции, дошедшие до каталога; `lib_demand.oem` пишется лишь при найденной шапке |
| цены из КП | `lib_prices` (`feed='разбор КП'`) тремя разрезами: `oem` — слово поставщика, `rfq_brands` — наша карточка, артикул → `lib_parts.oem` | есть; `rfq_brands` — ключи без имён |
| цены по таможне | `lib_customs.brand`, `usd_kg` | частично: цена за килограмм; с деталью сопоставлено 79–80 строк из 42 311 |
| отклик рынка на запросы по бренду | `lib_prices.rfq_brands` × `sup_fact(field='rfq_stats')` | нет: считался один раз (`docs/ПЛАН-КП-И-ЗАПРОСОВ.md:29-47`) |
| кто поставляет: OEM и дилеры | `lib_part_suppliers` с `source` «изготовитель (OEM) по каталогу»; `gt/data/ship_sellers.json`, `role` «дилер OEM» (43) | частично |
| кто поставляет: ODM-аналоги | `lib_part_suppliers` с `source='каталог ЗИП'` (4 291 ребро, только ГШО, `data/catalog_notes.json`); `gpu/data/analogs.json` (82 аналога с уровнем A/B/C) | частично: уровень происхождения не загружен |
| кто поставляет: трейдеры и все, кто давал КП | `lib_prices.rfq_company` → `sup_identifier(kind='bitrix')` → `sup_entity` | есть, но роль компании неизвестна |

Раздел «Кто поставляет» состоит из двух таблиц, которые не складываются:
«давал предложение по деталям этого бренда» (адрес по детали, с числом позиций) и
«заявляет работу с брендом» (родовой адрес: `lib_suppliers.oem_brands`, досье).
В счётчик и в заголовок идёт только первая.

### Раздел ODM-поставщика аналогов: поля и источники

| поле | источник | состояние |
|---|---|---|
| компания, страна, регион, сайт | `sup_entity`, `sup_identifier(kind='domain')`; `lib_suppliers.country`, `site`; `zip/data/odm_suppliers.json`: `country`, `region` | частично: `sup_entity.country` не заполняется; в ODM-реестре страна пуста у 656 строк |
| роль ODM и по каким брендам | `sup_fact(field='role')` с `scope.brand` | нет |
| какие бренды замещает | вывод: ребро `lib_part_suppliers` → `lib_parts.oem`; `lib_suppliers.oem_brands` | частично: вывод есть только по ГШО; `oem_brands` не пишет ни один загрузчик |
| какие коды замещает | `lib_part_suppliers` (`source='каталог ЗИП'`, 750 позиций); `lib_prices.part_number` у КП с аналогом | частично: у ODM-реестра нет номера аналога, только позиция |
| связи взаимозаменяемости | `lib_part_alt` (1 699; `kind`: номер изготовителя, замена, аналог, наш номер); `pnw/data/crossrefs.json` (753, из них analog 189); `gpu/data/analogs.json` | частично: `kind='аналог'` в базе пришёл только из MOTORTECH; два файла не загружены |
| уровень происхождения аналога | `gpu/data/analogs.json`, `level`: A — кросс от изготовителя компонента, B — независимый специалист, C — реверс-завод | частично: файл по ГПУ, 19 семейств |
| цены | `lib_prices` (`разбор КП`) по компании; `odm_suppliers.catalog_price` | частично: только у компаний из Bitrix; `catalog_price` пуст у всех 4 309 строк |
| сроки изготовления и поставки | `lib_prices.make_days`, `lead_days` с `make_src`, `lead_src`; `lib_suppliers.lead_time` | частично: по КП есть; у компании `lead_time` не пишется |
| наличие | `lib_part_suppliers.verdict`, `in_stock`, `stock_qty` | частично: 433 ребра, без покрытия количества и без даты проверки |
| MOQ, образцы, частный бренд | `odm_suppliers.json`, `terms` (заполнено у 1 780 строк); `lib_suppliers.moq` | частично: загрузчик `terms` не читает |
| сертификаты, ограничения поставки в РФ | `lib_suppliers.certificates`, `sanctions` | частично |
| оценка качества | таблица `odm_suppliers` базы ЗИП, скоркарта (`zip/supabase/migrations.sql:111-118`) | частично: только ЗИП; DDL самой таблицы в репозитории нет |
| история ответов на наши запросы | `sup_fact(field='rfq_stats')` (`scripts/supplier_responsiveness.py`) | частично: только у компаний из Bitrix; ODM из разведки с нашими запросами не связаны |

### Раздел трейдера: поля и источники

| поле | источник | состояние |
|---|---|---|
| роль (трейдер, независимый склад, маркетплейс, дилер) и по каким брендам | `sup_fact(field='role')`; `gt/data/ship_sellers.json`, `role` (507 строк) | нет в базе; в файле есть, при сведении теряется: `library/load_supplier_master.py` берёт только имя и сайт |
| какие бренды везёт — по своим КП | `lib_prices.oem` по `rfq_company` | есть |
| по каким брендам мы его запрашивали | `lib_prices.rfq_brands` по `rfq_company` | частично: ключи без имён |
| какие бренды везёт — по таможне | `lib_customs.exporter` × `brand` | частично: экспортёр — имя, с `sup_entity` не связан |
| что заявляет покрывать | `gt/data/ship_sellers.json`, `covers` | частично: файл, одна заявка |
| наличие, четыре состояния | `gt/data/ship_*.json`: `verdict`, `stock_grade`, `covers_qty`; `lib_part_suppliers` | частично: при переносе в базу теряются `covers_qty`, `stock_grade` и дата проверки |
| сроки | `lib_prices.make_days`, `lead_days` с источником | есть |
| цена, сумма, базис, оплата | `lib_prices.price`, `total`, `currency`, `basis`, `pay_terms`, `pay_advance_pct` с `*_src` | есть |
| происхождение товара: страна | в КП не извлекается; `lib_customs.origin`, `dispatch` | по КП нет; по таможне частично |
| что предложено: оригинал OEM, деталь под брендом изготовителя узла или аналог | в КП не извлекается; в `gt/data/ship_*.json` поле `kind` (`gt/tools/ship_map.py:31`) | по КП нет; частично по одной заявке |
| история котировок | `lib_prices` по `rfq_company` и `rfq_id`; `sup_fact(field='rfq_stats')` | частично: `price_date` разбор не пишет, карточка показывает дату разбора (`library/crossref.py:462`) |
| контакты | как в карточке `/suppliers`, право `suppliers_pii` | есть в карточке поставщика |
| договоры и деньги | `sup_terms` (этап 6), право `suppliers_fin` | нет |

### Не терять информацию

Каждый факт из КП и из файлов поставщика привязывается хотя бы к одной из двух
карточек — бренда или компании, лучше к обеим. Если компания не установлена,
факт остаётся у бренда с пометкой «поставщик не установлен». Если не установлен
бренд — у компании с пометкой «бренд не установлен». Факт, не привязанный ни к
одной карточке, не отбрасывается: он считается потерянным и виден числом в
счётчике.

Что теряется сейчас и что для этого дописать:

| что теряется | где | что дописать |
|---|---|---|
| компания и бренды у строк КП без цены: такие строки уходят в `lib_demand` с `source='котировка поставщика'` | `library/indexer.py` | `rfq_company` и `rfq_brands` в `lib_files`: файл принадлежит одной карточке запроса, и строки без цены привязываются через него. Обе вставки `flush()`, пакетная и построчная, правятся вместе и закрываются тестом (правило 14) |
| что предложено: оригинал OEM, деталь под брендом изготовителя узла или аналог | разбор КП этого не извлекает | `lib_prices.offer_kind` и `offer_kind_src` значениями `library/offer_terms.py` (строка / файл / нет / несколько / не проверено), как у базиса и оплаты |
| страна происхождения | не извлекается. Слово `origin` стоит в `СЛОВА_ЗАМЕРА` у колонки изготовителя (`library/indexer.py:816`): при переносе в разбор «Country of origin» ляжет в `oem` | `lib_prices.origin` и `origin_src`; из слов изготовителя `origin` убрать |
| срок действия предложения, гарантия | распознаются только затем, чтобы не спутать их со сроком поставки (`offer_terms._ЧУЖОЙ_СРОК`) | `valid_days`, `warranty` с источником |
| MOQ и остаток на складе, названные в КП | не извлекаются | `moq`, `stock_qty`, `stock_src` |
| дата предложения | `price_date` не заполняется (`library/price_store.py`) | дата документа или письма; если её нет — пусто и `date_src`, а не дата разбора |
| предложения поставщиков в полях сделки: 432 016 строк, 69 361 код (`lib_files.side='поставщик'`) | в режиме `SOURCE=deals` цена не пишется (`library/indexer.py:2201`), компании нет | цену писать отдельным `feed` с пустой компанией: факт привязывается к коду и бренду, «поставщик не установлен» считается числом |
| роль из `ship_sellers.role` | `library/load_supplier_master.py` берёт только имя и сайт | факт `sup_fact(field='role', status='stated')` |
| тип предложенного товара из `ship_lukoil`, `ship_sweep`, `ship_energoseti` | `library/load_suppliers.py` пишет его как роль компании (`kind`) | на ребро или на строку цены, а не на компанию |
| MOQ, частный бренд, образцы из ODM-реестра | `library/load_suppliers.py` не читает `terms` и `region` | `lib_suppliers.moq` и факты с источником |
| покрытие объёма, степень остатка, дата проверки | `library/load_parts.py` при переносе проверок наличия | этап 3, «перенос трёх осей наличия целиком»; здесь не дублируется |
| кроссы и аналоги из файлов | `pnw/data/crossrefs.json`, `gpu/data/analogs.json` не загружены | в `lib_part_alt` с источником и уровнем происхождения |

Правка разбора идёт обычным порядком: холостой прогон, сетка порогов за один
проход, запись только с ключом прогона. Кроме итога считается, у скольких файлов
стало хуже (правило 0), и такие файлы запись пропускает (`APPLY_ONLY_BETTER`).
Этап 3 тоже трогает разбор КП (одно правило нормализации артикула), поэтому
обе правки сводятся в один переразбор, а не в два подряд.

### Признак типа поставщика

**Словарь один.** Оба прежних словаря и 241 значение `lib_suppliers.kind`
отображаются в него закрытой таблицей в коде. Незнакомое значение получает «не
определена» и считается числом, а не угадывается. `sup_entity.roles` становится
проекцией фактов (видом); ни один загрузчик не пишет в него напрямую.

| роль | что значит | относительно чего |
|---|---|---|
| `oem` | владелец конструкции, продаёт под своим брендом | бренда |
| `dealer` | авторизованный продавец OEM | бренда |
| `component_maker` | реальный изготовитель узла под брендом OEM (`docs/ПЛАН-КП-И-ЗАПРОСОВ.md`, §1.9) | бренда и узла |
| `odm` | изготовитель аналога под своим или частным брендом | бренда, который замещает |
| `distributor` | официальный канал изготовителя компонента | бренда изготовителя |
| `trader` | перепродаёт без авторизации: независимый склад, торговец | — |
| `marketplace` | площадка штучных лотов | — |
| `service` | ремонт и восстановление | — |
| `logistics` | перевозка и таможенное оформление | — |

**Роль — факт, а не колонка:** `sup_fact(subject_kind='entity', field='role',
value, scope={"brand": …}, status, source_type, method, method_ver, run_id)`.
Причин три: у роли есть источник и степень доказанности; роль у одной компании
разная по брендам; противоречие двух источников должно быть видно, а не
затираться правилом «первая запись навсегда», как сейчас у `lib_suppliers.kind`
(`library/load_suppliers.py:243`).

**Кто ставит и с каким статусом:**

- загрузчик из разведки или из файла самой компании — `stated`: это слово
  разведки или компании;
- вывод по поведению — `inferred`. Таможня (экспортёр не совпадает с брендом, у
  экспортёра много брендов, страна отправления отличается от страны
  происхождения) даёт `trader`. КП с `offer_kind='аналог'` под собственным брендом
  компании даёт `odm`. Весь ODM-реестр идёт как `odm` со статусом `candidate`:
  у всех 4 309 его строк статус «кандидат»;
- человек в очереди `sup_review` с правом `suppliers_edit` — `verified`, только с
  доказательством в `evidence`.

**Чем подтверждается `verified`:**

- `dealer` — страница дилерской сети OEM или письмо OEM;
- `oem` и `component_maker` — собственный каталог с номерами или производственная
  площадка; для изготовителя узла — ребро `dict/chain.json` с `proof`;
- `odm` — каталог с кроссами на номера OEM и хотя бы одна наша котировка или
  образец;
- `trader` — умолчание для компании, давшей цену без подтверждения производства.
  По одному КП роль не ставится.

Если два источника дают разные роли по одному бренду, противоречие не
разрешается молча: оно уходит строкой в `sup_review` с `kind='role_conflict'`.
Новый вид добавляется в `check` отдельным `alter` (правило 21). Роль со статусом
`candidate` в подборе адресатов (этап 5) веса не имеет.

**Роль компании и тип предложения — разные вещи.** Трейдер может предложить
оригинал, изготовитель узла — деталь под своим брендом. `offer_kind` лежит на
строке цены, роль — на компании, и по одной котировке одно из другого не
выводится.

**Родовой адрес и деталь.** «Компания работает с брендом» (роль, `oem_brands`,
досье) и «у компании есть эта деталь» (КП по коду, проверка наличия по коду) —
разные утверждения (`CLAUDE.md`, «Числа в выгрузках владельцу»;
`DATA_SOURCE_MAP.md`, строка «Адресат по детали»). В карточках это разные
таблицы. В заголовок, в счётчик и в сумму идёт только второе.

**Наличие — четыре состояния, а не одно:** подтверждённый остаток на весь объём
(`covers_qty='full'` с датой проверки); наличие без покрытия количества;
«отгрузим, если есть»; запись прошлой проверки, не перепроверенная сейчас. Отдельно
от них и друг от друга — два отказа: «номер не найден» и «только у OEM»
(`lib_part_suppliers.verdict`). В сумму закупки идёт только первое состояние.

### Публичность

Репозиторий публичный, а pull request'ы находятся поисковыми машинами по номеру
детали (`SECURITY.md`, замер 18.09.2026). Решение Р-1: данные поставщиков только
за входом, в git ничего.

- Имена поставщиков, цены, роли конкретных компаний, их бренды и коды хранятся
  только в Supabase и в снимках KV за Cloudflare Access с правом `suppliers`.
  Контакты режутся правом `suppliers_pii`, условия и деньги — `suppliers_fin`, на
  сервере (`PERMISSION_MATRIX.md`).
- В репозиторий, журналы Actions, описания PR и `STATE.md` попадают только
  агрегаты: счётчики, доли, названия сегментов, собственные константы кода
  (правило 17). Разбивка по брендам с числами спроса и цен — тоже только в
  снимке: имя бренда публично, а спрос по нему нет.
- Корпуса тестов придумываются (правило 18).
- Новых наборов с именами поставщиков в репозиторий этот этап не добавляет.
  Загрузка в базу уже лежащих файлов (`zip/data/odm_suppliers.json`,
  `gt/data/ship_sellers.json`) их публичности не меняет; решение по ним за
  владельцем (`SECURITY.md`).

### Части этапа

Каждая часть — отдельный PR с проверяемым результатом. Каждый прогон, который
обходит базу или вложения, принимает число частей входом (10, 25 или 50), и
разбиение в нём ровно одно (правило дробления).

Счётчик на этап один — `scripts/card_coverage.py` по образцу
`gpu/tools/coverage.py`. По каждой карточке и каждому полю он считает, у скольких
сущностей поле заполнено, долю и статус: «закрыто» от 80 %, «частично» от 40 %,
ниже — «дыра». Отдельно — сколько фактов не привязано ни к бренду, ни к
компании. В журнал идут только эти агрегаты. Те же числа кладутся в снимок и
показываются на страницах, а в репозиторий попадают записью в `STATE.md`.

**8.1. Срез на имеющихся данных, без записи в базу (12–18 ч).** Карточка бренда
`/brands`: чистая сборка `library/brands.py`, публикатор `scripts/publish_brands.py`
в KV `brands:v1` (отдельный ключ, в закрытом списке ключей публикатора), маршрут и
`/api/brands` в `public/_worker.js` с правом `suppliers`. Ключ бренда берётся из
`dict/oem.json` при сборке снимка. Неразрешённое написание не отбрасывается, а
показывается числом «имя без ключа». Источники — только то, что уже есть:
`lib_models`, `lib_part_models`, `lib_parts`, `lib_units`, `lib_demand_catalog`,
`lib_prices`, `sup_entity`; `dict/chain.json` и `zip/data/oem_atlas.json` читаются
из файлов. В карточке компании на `/suppliers` — раздел «Бренды»: бренды по слову
поставщика отдельно от брендов нашей карточки запроса, со ссылкой на карточку
бренда. У `/nomenclature`, `/suppliers` и `/brands` появляется прямой адрес
записи после `#`, как у `/library`: без него карточки не сошлются друг на друга.
**Готово, когда:** по каждому бренду, у которого в каталоге есть детали, карточка
показывает машины, узлы, число деталей, спрос и предложения; у каждого поля
подписан источник, а доля «не определено» видна числом; счётчик печатает
заполненность по полям; размер снимка измерен и записан вместе с запасом до
предела.

**8.2. Реестр брендов в базе (10–16 ч).** `lib_brands`: ключ, имя, владелец,
прежние имена, страна, элемент СП-176. `lib_brand_alias`: написание → ключ, где
встречено, статус, `run_id`. Засев из СП-176 (прогон в Actions), `dict/oem.json`,
`zip/data/oem_atlas.json` и `OEM_ALIAS`. Вместо пяти правил нормализации — одно,
с версией; прежние сверяются с ним на одном корпусе написаний до замены. Бренд
разрешается видом через таблицу написаний, без `UPDATE` больших таблиц
(правило 5): в `lib_demand` 1,45 млн строк. Ключи `rfq_brands` разрешаются по
элементу СП-176.
**Готово, когда:** доля строк цены с разрешённым брендом измерена по каждому из
трёх разрезов; каждое неразрешённое написание стоит в очереди, а не пропадает;
повторный засев не создаёт дублей.

**8.3. Словарь ролей и роль как факт (10–15 ч).** Словарь и таблица соответствия
из раздела выше. Засев фактов: `gt/data/ship_sellers.json` (`role`), уровни
компаний на страницах машин ГТУ (`tier`), весь ODM-реестр (`candidate`). Починка
`library/load_suppliers.py`: тип предложенного товара из `ship_*` больше не
пишется компании как роль.
**Готово, когда:** у каждого факта роли есть источник, метод, версия метода и
`run_id`; счётчик печатает долю компаний с ролью по статусам; тест на придуманном
корпусе: «дилер» из одного источника и «трейдер» из другого по одному бренду дают
строку `role_conflict`, а не перезапись.

**8.4. Сведение исполнителей разведки с реестром портала (8–14 ч).** Доводит
п. 3 этапа 1 до `lib_suppliers`, которого сведение пока не читает.
`lib_suppliers` (4 535) и ODM-реестр (1 289 разных имён) связываются с
`sup_entity` строками `sup_identifier(kind='reestr')` по правилу этапа 1: совпал
домен — `verified`; совпало имя и домены не противоречат — `inferred`; только имя —
`candidate`. Склейки в `verified` по одному имени нет. Сначала пересечение
меряется таблицей, как в этапе 1, п. 4, и только потом пишется.
**Готово, когда:** пересечение измерено числом; у ODM-поставщиков и трейдеров
из разведки со связью `verified` или `inferred` карточка показывает и роль, и
наши котировки; связь `candidate` показывается отдельно, как «возможно, та же
компания».

**8.5. Разбор КП: факты, которые теряются (15–25 ч).** Колонки и правки из
таблицы «Не терять информацию»: `offer_kind`, `origin`, срок действия, гарантия,
MOQ, остаток, дата предложения, у каждого поля источник значениями
`offer_terms`; `rfq_company` и `rfq_brands` в `lib_files`; цена из полей сделки
отдельным потоком. Переразбор 7 523 вложений КП — 25 частей, поля сделки — 50.
**Готово, когда:** по каждому новому полю напечатана доля строк по источнику
(строка / файл / нет / несколько / не проверено); файлы, у которых цен стало
меньше, пропущены и посчитаны; строки без цены привязаны к компании у той же доли
файлов, что и строки с ценой.

**8.6. Файлы, которые лежат мимо базы (8–12 ч).** `pnw/data/crossrefs.json` (753)
и `gpu/data/analogs.json` (82, с уровнем A/B/C) — в `lib_part_alt` с уровнем
происхождения. `dict/chain.json` (311) и `zip/data/subsupplier_recon.json` (150) —
рёбрами «бренд → узел → изготовитель узла» с областью (машинами) и
доказательством. Сюда же узлы машин ГШО из `zip/data/machines.json`, а также
`terms` и `region` ODM-реестра.
**Готово, когда:** загружено столько, сколько в файле, за вычетом отвергнутых, и
у каждого отвержения названа причина; повторный прогон не создаёт дублей.

**8.7. Разделы ODM и трейдера в карточке компании и два списка (12–20 ч).** Поля —
из таблиц выше. Разделы включаются фактом роли, списки фильтруются по бренду и по
статусу роли.
**Готово, когда:** в списке ODM у каждой компании видно, какие бренды и коды она
замещает и чем это подтверждено; в списке трейдеров видны бренды, наличие
четырьмя состояниями и история котировок; ни одно родовое утверждение не стоит в
заголовке или в сумме.

**8.8. Трейдер по таможне — выведенные признаки (6–10 ч).** Разрез «бренд ×
экспортёр» по всем 42 311 строкам, без привязки к детали. Признаки: экспортёр не
совпадает с брендом, число брендов у экспортёра, страна отправления отличается от
страны происхождения. Порог выбирается по сетке, посчитанной за один проход
(правило 4). Факты ставятся только со статусом `inferred`; экспортёр связывается с
`sup_entity` по правилу 8.4. Источник платный (glbs.io), поэтому показывается
только за входом.
**Готово, когда:** сетка порогов напечатана таблицей; при выбранном пороге
измерены число экспортёров-трейдеров и доля, связанная с реестром.

**Порядок внутри этапа.** 8.1 ничего не ждёт и начинается первой. 8.2, 8.3, 8.5 и
8.6 друг от друга не зависят и идут параллельно. 8.4 опирается на этап 1. 8.7
ждёт 8.3 и 8.4, 8.8 — 8.3. В этап 5 карточки входят после 8.2 и 8.3: без реестра
брендов и роли уровни «verified manufacturer» и «изготовитель узла» подбору
адресатов нечем наполнять.

## Порядок и зависимости

```
0 ──▶ 1 ──▶ 2 ──▶ 3 ──▶ 4
             │      └──▶ 5 ◀──┐
             ├──▶ 6           │
             └──▶ 8 ──────────┘
      1 ──────────▶ 7
      1 ──────────▶ 8
```

Этап 0 — до всего: без него считается корпус из пятидесяти карточек.
Этапы 4 и 5 идут после 3: без модели котировки таблице нечего показывать.
Этап 7 можно вести параллельно с 4–5.
Этап 8 стоит на 1 (`sup_id`) и 2 (раздел `/suppliers` и снимок `crossref:v1`) и
кормит 5: реестр брендов и роль компании наполняют уровни «verified
manufacturer» и «изготовитель узла». Его часть 8.1 можно начинать сразу.

## Риски

| риск | вероятность | что делаем |
|---|---|---|
| сведение реестров даёт много кандидатов и мало слияний | высокая | это нормально: кандидат лучше ошибочного слияния. Меряем числом, порог не подкручиваем задним числом |
| правило нормализации артикула ломает существующие связи | средняя | обратная совместимость проверяется на корпусе до включения; старый ключ хранится рядом |
| нет staging — правка едет сразу в прод | высокая | раздел за флагом до этапа 2; каждый мерж — деплой (`CLAUDE.md`, правило 2) |
| данные ЛУКОЙЛ окажутся единственным корпусом с тремя осями наличия | высокая | так и есть: 1 642 артикула из одной заявки. Не выдавать за общую статистику |
| владелец не назовёт юрлица | средняя | колонки заложены, интерфейс не показывает разрез до наполнения |
| роль, выведенная по поведению или из разведки, читается как проверенная | высокая | статус роли стоит в карточке рядом с ней; `candidate` и `inferred` в подборе адресатов не равны `verified` (этап 8) |
| исполнителя из разведки склеят с компанией портала по одному имени | средняя | запрещено: одно имя даёт только `candidate` (8.4) |
| карточки брендов переполнят снимок | средняя | отдельный ключ `brands:v1`, размер меряется каждой публикацией, корзины — по плану из `library/crossref.py` |
| разбивка по брендам со спросом и ценами попадёт в журнал или в описание PR | средняя | счётчик печатает только агрегаты; тест на его вывод по придуманному корпусу |

## Чего в плане намеренно нет

Платежи, фактические поставки, качество, рекламации. Источника нет, схему под
них заводим вместе с первым источником — пустая таблица выглядит фактом.

## Блокер по пункту 0.2: нечем измерить

Перевод ключа поставщика с текста названия на `rfq.supplier_id` написать можно,
проверить — нельзя. `base/kvant.db` в репозиторий не коммитится
(`.gitignore:41-42`), а эффект перевода целиком зависит от данных:

- сколько карточек СП-166 имеют `supplier_id = 0` (поле пустое) — они схлопнутся
  в одну корзину, если не задать обратный ход на нормализованное имя;
- сколько написаний названия приходится на один `companyId` — от этого зависит,
  во сколько раз сократится число «поставщиков»;
- какое написание брать канонически для отображения.

При этом ключ читают ещё три таблицы, которых перевод коснётся:
`base/quote.py:58` джойнит `supplier_stats` к `supplier_prices` по тексту имени,
`:67` — к `brand_suppliers`, и обе построены `match_prices.py` и `file_cards.py`
из своих текстовых колонок. Перевод одной таблицы без остальных разорвёт джойн:
вариантное написание перестанет находиться вовсе, а сегодня оно находит хотя бы
расщеплённую строку.

`CLAUDE.md`, правило 3: правило сначала меряют, потом применяют; калибровка на
придуманном корпусе показывает верхнюю границу качества, а не рабочую. Поэтому
0.2 переносится в этап 1, где сведение реестров делается один раз, с замером
и таблицей соответствий имён, а не двумя несогласованными правками.

**Разблокировано 20.09.2026 (2):** владелец подтвердил, что `SUPABASE_SERVICE_KEY`
в Cloudflare задан, и разрешил правку `public/_worker.js` и `access/acl.js`.
Замер переписан на чтение портала напрямую: `kb_rfq` в Supabase не существует.

**Разблокировано 20.09.2026:** замер написан — `scripts/supplier_merge_stats.py`,
запускается кнопкой (Actions → «Поставщики — замер сведения») и читает выгрузку
`kb_rfq` в Supabase, поэтому локальная база не нужна. Ждёт запуска владельцем.
Он отвечает ровно на четыре вопроса этого раздела и печатает только агрегаты.

Прежние варианты разблокировки:
1. прогон `base/suppliers.py` на живой базе с холостым замером — сколько
   сущностей до и после, сколько карточек без `supplier_id`, распределение
   написаний на один `companyId`;
2. либо сразу этап 1: таблица `sup_identifier` как единственное место сведения,
   и все четыре таблицы `base/` смотрят в неё, а не друг на друга.

## Блокер по пункту 0.6: гейт покраснеет на нашем же ключе

Научить гейт шаблонам `sb_publishable_` и `sb_secret_` нельзя, пока публикуемый
ключ лежит в рабочем дереве: `zip/site/_worker.js:63`. Добавленный шаблон найдёт
его первым же прогоном и покрасит гейт на КАЖДОМ pull request, включая чужие.

Обходить исключением на эту строку — плохо: исключение переживёт удаление ключа и
перестанет защищать ровно то место, где он был.

Правильный порядок — тот же, что у ужесточения доступа к базе ЗИП, и первые два
шага общие:

1. задать `SUPABASE_SERVICE_KEY` в Cloudflare для проекта `kvant-zip`;
2. убедиться, что сайт ЗИП работает (воркер пойдёт сервисной ролью);
3. убрать `SUPA_FALLBACK_KEY` из `zip/site/_worker.js`;
4. **только теперь** добавить шаблоны в гейт — ему больше нечего находить.

Готовность к шагам 1–3 меряется `scripts/zip_rls_readiness.py`.

## Пункт 0.4: сделано второе лучшее, а не заявленное

Опознавать стадии по `stageId` вместо отображаемого имени нельзя: коды воронок
смарт-процесса задаются в портале (вида `DT166_16:UC_XXXXX`) и из репозитория
неизвестны. Поэтому сделано то, что закрывает саму опасность: неопознанная
стадия теперь СЧИТАЕТСЯ и попадает в сводку прогона числом и списком имён, а не
проваливается молча. Плюс заработал разряд `LOST` — он был объявлен и не
использовался, из-за чего карточка «выбрали не его» была неотличима от карточки
с переименованной стадией.

Чтобы закрыть пункт целиком, нужен список стадий из портала: его отдаёт
`crm.status.list` для `DYNAMIC_166_STAGE*` (уже вызывается в
`base/fetch_rfq.py:103`). Достаточно сохранить `stageId` рядом с именем в разряды
— один прогон с сетью.
