-- ОСВОБОЖДЕНИЕ МЕСТА, КОГДА БАЗА ВСТАЛА В РЕЖИМ ТОЛЬКО ЧТЕНИЯ
--
-- 23.09.2026 запись переразбора упала на «cannot execute INSERT in a read-only
-- transaction»: 39 частей из 50 записали, остальные нет. Замер подтвердил:
-- transaction_read_only = on, реплика — нет, база 1500 МБ, таблицы public 1296 МБ.
-- У Supabase это состояние кончившегося диска.
--
-- ЭТОТ ФАЙЛ НЕ ПРИМЕНЯЕТСЯ ПРОГОНОМ И НЕ ВХОДИТ В МИГРАЦИИ. Удаление данных и
-- снятие индексов — решение владельца (CLAUDE.md, «стоять нельзя», исключения).
-- Здесь собрано то, что нужно решать: имена, размеры и цена каждого шага.
--
-- ПОРЯДОК РАБОТЫ (документация Supabase, «Understanding Database and Disk Size»):
--   1. Открыть SQL Editor в панели проекта.
--   2. Разрешить запись в своей сессии:  set session characteristics as transaction read write;
--   3. Выполнить нужные шаги ниже.
--   4. vacuum;   -- вернуть освобождённое место
--   5. Снять режим только чтения:        set default_transaction_read_only = 'off';
--
-- ЛИБО, без удаления вовсе: расширить диск в панели, Database Settings. Там же
-- сказано, что изменений диска разрешено не больше четырёх за сутки.

-- ─────────────────────────────────────────────────────────────────────────────
-- ШАГ 1. ИНДЕКСЫ, КОТОРЫМИ НИ РАЗУ НЕ ПОЛЬЗОВАЛИСЬ.  Освобождает ~112 МБ.
--
-- Замер 23.09.2026, pg_stat_user_indexes. Число обращений накоплено настоящее:
-- у lib_demand_pkey за тот же период 414 130 обращений, значит ноль — это ноль,
-- а не сброшенная статистика.
--
--   lib_demand_fts        65 МБ   0 обращений
--   lib_demand_pnkey      24 МБ   0 обращений
--   lib_knowledge_fts     17 МБ   0 обращений
--   lib_suppliers_fts    5,7 МБ   0 обращений
--
-- ЧЕМ ПРИДЁТСЯ ЗАПЛАТИТЬ. Это полнотекстовые индексы. Ими не пользуются СЕЙЧАС,
-- но они и поставлены под будущий поиск по библиотеке. Снять их — значит отложить
-- поиск до того дня, когда место появится: пересоздаются они одной командой, но
-- на 2,3 млн строк это не минута. lib_demand_pnkey (ключ артикула) к поиску
-- отношения не имеет и не используется ничем — этот снимается без последствий.
--
-- Начать стоит с pnkey: 24 МБ без единого довода против.

-- drop index if exists lib_demand_pnkey;      -- 24 МБ, ничем не используется
-- drop index if exists lib_suppliers_fts;     -- 5,7 МБ, поиск по поставщикам
-- drop index if exists lib_knowledge_fts;     -- 17 МБ, поиск по знаниям
-- drop index if exists lib_demand_fts;        -- 65 МБ, поиск по спросу

-- НЕ ТРОГАТЬ индексы таблицы storage.objects (~34 МБ с нулём обращений:
-- idx_objects_bucket_id_name, idx_objects_bucket_id_name_lower). Это таблица
-- платформы Supabase, а не проекта: она их создаёт сама и создаст заново.

-- ─────────────────────────────────────────────────────────────────────────────
-- ШАГ 2. ПОМЕТКИ ЗАВЕРШЁННЫХ ПРОГОНОВ РАЗМЕТКИ.  Освобождает до 70 МБ.
--
-- lib_row_junk — 70 МБ, 412 404 строки. Это записи о том, какие строки спроса
-- помечены мусором, с ключом прогона: по ним разметка откатывается (CLAUDE.md,
-- правила 5 и 6). Пока прогон может понадобиться к откату, его строки нужны.
--
-- Сначала ПОСМОТРЕТЬ, какие прогоны там лежат и насколько они старые:

-- select run_id, count(*) as строк, min(marked_at) as с, max(marked_at) as по
--   from lib_row_junk group by run_id order by min(marked_at);

-- И только потом удалять поимённо тот, который заведомо не нужен. Массового
-- удаления здесь намеренно нет: пометка снимается одной командой, а удалённую
-- строку не вернуть.

-- ─────────────────────────────────────────────────────────────────────────────
-- ШАГ 3. ВЕРНУТЬ ОСВОБОЖДЁННОЕ.
--
-- Обычный vacuum помечает место пригодным для повторного использования внутри
-- базы; операционной системе оно возвращается только через vacuum full, а тот
-- держит таблицу под замком всё время работы и требует места под копию. На
-- lib_demand (855 МБ) этого места сейчас может не быть — потому сначала vacuum,
-- и только при нехватке, по свободному диску, vacuum full поимённо.

-- vacuum;
-- vacuum (analyze) lib_demand;

-- ─────────────────────────────────────────────────────────────────────────────
-- ПОПРАВКА К РАНЕЕ СКАЗАННОМУ. Сперва я записал, что у lib_knowledge «143 МБ
-- индексов на 41 МБ данных». Это неверно: pg_total_relation_size минус
-- pg_relation_size — это индексы ПЛЮС TOAST, а индексов там всего 17 МБ.
-- Остальные ~126 МБ — сами длинные тексты в TOAST, и снятием индекса они не
-- освобождаются. Освобождает их только удаление записей.
