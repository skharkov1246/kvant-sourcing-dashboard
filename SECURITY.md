# Данные и доступ

Репозиторий **публичный**. Всё, что здесь лежит, читается кем угодно, включая
историю коммитов. Этот файл фиксирует, что именно открыто, что уже исправлено
и какие решения остаются за владельцем.

## Что открыто сейчас

Полный список — в `data/catalog.json` (пересобрать: `python scripts/build_catalog.py`).
Сводка на 04.09.2026: 158 наборов данных, 69,5 МБ, из них **43 помечены как
конфиденциальные**.

| Набор | Объём | Что внутри |
|---|---|---|
| `zip/customs/out/*.json` | 33 МБ | 42 311 таможенных деклараций: ИНН импортёров, экспортёры, стоимости. Источник — платная подписка glbs.io |
| `data/budget_snapshot.json` | 245 КБ | 352 бюджета сделок: заказчик, исполнитель, выручка, закупка, маржа. Σ выручки ≈ 3,9 млрд ₽ |
| `data/chat_snapshot.json` | 507 КБ | Ежедневные метрики реакции 15 сотрудников и LLM-оценки их работы |
| `gt/data/dossiers.json` | 1,6 МБ | 1 359 досье поставщиков: 731 e-mail, 992 телефона |
| `ove/public/docs/` | ~13 МБ | 495 документов ОВЭ-75, включая тендерный пакет и черновики заявки |
| `zip/data/supplier_crm.json` | — | Стадии переговоров и комментарии по поставщикам |

Отдельно: публикуемый ключ Supabase в `zip/site/index.template.html` вместе с
политиками `for all to anon using (true)` даёт прямой доступ к базе ЗИП на чтение,
запись и удаление в обход интерфейса.

## Что исправлено в этом изменении

- Из индекса git убран `.wrangler/cache/` — он раскрывал `account_id` Cloudflare
  и e-mail владельца; добавлен в `.gitignore`.
- Убраны мёртвые артефакты `scripts/_coll_*.json`.
- В CI-гейт добавлен поиск секретов: вебхуки Bitrix с токеном, ключи Anthropic,
  GitHub, AWS и приватные ключи не пройдут в PR.
- Каждый набор данных получил оценку чувствительности в каталоге, поэтому новый
  конфиденциальный файл виден сразу, а не через год.

Важно: удаление файла из рабочего дерева **не удаляет его из истории**. Всё
перечисленное выше остаётся доступным в старых коммитах, пока история не очищена.

## Решения владельца

Ни одно из этих действий не выполнено автоматически: каждое либо ломает
работающий сайт, либо требует доступа, которого у агента нет.

1. **Закрыть репозиторий или вынести данные.** Репозиторий сделан публичным ради
   бесплатных минут GitHub Actions (расход ≈3 600 мин/мес при лимите 2 000 для
   приватных). Варианты: приватный репозиторий с оплатой минут; вынос данных в
   приватный репозиторий-спутник или Cloudflare R2 с подгрузкой в CI по секрету.
   *Рекомендация: вынести данные, репозиторий кода оставить публичным.*

2. **Ограничить доступ к базе ЗИП.** Заготовка — `zip/supabase/migrations_rls.sql`,
   два варианта с описанием последствий. Сейчас не применено, потому что сайт
   пишет в базу тем же ключом и запрет записи его сломает.
   *Рекомендация: вариант A сразу, вариант B — при следующей правке сайта.*

3. **Очистить историю и отозвать учётные данные.** После выноса данных —
   `git filter-repo` по перечисленным путям, затем пересоздать вебхук Bitrix,
   ключ glbs.io и пароль Basic Auth. Это переписывает историю: нужно согласовать
   момент и предупредить всех, у кого есть клоны.

4. **Проверить условия подписки glbs.io** на перепубликацию выгрузки. Если
   перепубликация запрещена, пункты 1 и 3 становятся срочными.

## Куда класть секреты

Только в GitHub Secrets (Settings → Secrets and variables → Actions) и в секреты
Cloudflare Pages. В коде, в комментариях к workflow и в данных — никогда.
Локально — в `.env`, который в `.gitignore`.
